{% extends 'base.html.twig' %}
{% block title %}Administration — Horaires{% endblock %}
{% block body %}
<div style="max-width:800px;margin:2rem auto;padding:0 1rem;">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem;">
        <h1 style="font-size:1.8rem;font-weight:700;">🕐 Gestion des Horaires</h1>
        <a href="{{ path('admin_menu_index') }}" style="color:#6b7280;text-decoration:none;">← Espace admin</a>
    </div>

    {% for flash in app.flashes('success') %}
        <div style="background:rgba(16,185,129,0.15);border:1px solid rgba(16,185,129,0.4);color:#065F46;border-radius:0.5rem;padding:1rem;margin-bottom:1rem;">✅ {{ flash }}</div>
    {% endfor %}

    <div style="display:grid;gap:1rem;">
        {% for horaire in horaires %}
        <div style="background:white;border-radius:1rem;box-shadow:0 2px 8px rgba(0,0,0,0.06);padding:1.25rem;display:flex;justify-content:space-between;align-items:center;">
            <div>
                <span style="font-weight:700;font-size:1.1rem;text-transform:capitalize;">{{ horaire.jour }}</span>
                <span style="margin-left:1rem;color:{% if horaire.ferme %}#ef4444{% else %}var(--primary-color){% endif %};">
                    {% if horaire.ferme %}Fermé{% else %}{{ horaire.heureOuverture }} – {{ horaire.heureFermeture }}{% endif %}
                </span>
            </div>
            <a href="{{ path('admin_horaire_edit', {id: horaire.id}) }}" style="background:rgba(99,102,241,0.1);color:#6366f1;padding:0.4rem 0.8rem;border-radius:0.4rem;text-decoration:none;font-size:0.85rem;">✏️ Modifier</a>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
